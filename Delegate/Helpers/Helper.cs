namespace Delegate.Helpers
{
    //public delegate bool Check(int m);
    //public delegate K Print<in T, in U, out K>(T word1, U word2);
    public delegate void Print(string message);


    public delegate int Calculate(int n, int m);
}
